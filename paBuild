# Compiles the given .c file using portaudio and outputs to the next given file name
# This script must be called from within its parent directory
gcc $1 ./portaudio/lib/.libs/libportaudio.a -lrt -lm -lasound -ljack -pthread -o $2
